import { IsInvalidFormDirective } from './is-invalid-form.directive';

describe('IsInvalidFormDirective', () => {
  it('should create an instance', () => {
    const directive = new IsInvalidFormDirective();
    expect(directive).toBeTruthy();
  });
});
